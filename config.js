/* =====================================================================
   config.js — public Supabase settings for Star Learner.

   These two values are PUBLIC by design (the "publishable"/anon key is meant
   to live in client code). Your data is protected by Row-Level Security in
   the database, NOT by hiding this key — so it is safe to commit and to ship
   to the browser.

   To point the app at a different Supabase project, change these two lines.
   ===================================================================== */
window.SL_CONFIG = {
  url: "https://iqqludtsootlcbrucfiu.supabase.co",
  anonKey: "sb_publishable_3JyRJN_biqj54tnYMUxS0w_NiRIqoTG",
};
